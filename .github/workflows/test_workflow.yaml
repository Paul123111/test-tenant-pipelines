name: test-workflow
on: 
  schedule:
  - cron: '*/5 * * * *'

jobs:
  job1:
    name: Development
    runs-on: ubuntu-latest

    steps:
    - name: Git checkout
      uses: actions/checkout@v2
      with: { ref: development }
    - name: List files
      run: ls
